fastapi==0.110.0
uvicorn[standard]==0.28.0
sqlalchemy==2.0.28
alembic==1.13.1
psycopg[binary]==3.1.18
pgvector==0.2.5
pydantic-settings==2.2.1
pydantic[email]==2.6.4
email-validator>=2.0.0
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
slowapi==0.1.9
google-generativeai>=0.4.1
python-dateutil==2.9.0.post0
# IANA zone database for `zoneinfo`. Linux images usually carry the system one,
# but Windows never does and a slim image may not either — without it
# ZoneInfo("Asia/Jerusalem") raises and every wall clock silently falls back to
# UTC, which is the bug the timezone handling exists to prevent.
tzdata>=2024.1
apscheduler>=3.10.4
websockets>=12.0
pytest==8.1.1
pytest-asyncio>=0.23.5
httpx==0.27.0
python-multipart>=0.0.9
